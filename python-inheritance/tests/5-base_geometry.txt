================================
Test file for BaseGeometry
================================

Import the class:
    >>> BaseGeometry = __import__('5-base_geometry').BaseGeometry

Check instantiation:
    >>> bg = BaseGeometry()
    >>> isinstance(bg, BaseGeometry)
    True

Check documentation (sanity check):
    >>> len(BaseGeometry.__doc__) > 0
    True